# VibePod 開発ルール

## コードフォーマット

- コミット前に必ず `cargo fmt` を実行すること。CI で `cargo fmt --check` が走るため、フォーマットが崩れていると CI が落ちる
- `cargo clippy` の警告も解消すること

## エラーハンドリング

- `unwrap()` の使用は regex コンパイル（パターンがリテラルで正しいことが保証されている場合）以外では避けること。代わりに `?` 演算子、`expect()` (パニックが設計上あり得ない場合)、または適切なエラーハンドリングを使用すること
