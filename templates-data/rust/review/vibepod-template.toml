# Rust review bundle — read-only evaluation of Rust code. Reviewer agents +
# santa-method dual-convergence skill. Complements the rust/impl bundle.
#
# Pulled skills/agents are sourced from ~/.config/vibepod/ecc-cache/ at
# container start; missing entries fail-fast.

[runtime]
required_langs = ["rust"]

[ecc]
skills = [
  "skills/santa-method/SKILL.md",
]
agents = [
  "agents/rust-reviewer.md",
  "agents/security-reviewer.md",
  "agents/silent-failure-hunter.md",
  "agents/code-reviewer.md",
]
